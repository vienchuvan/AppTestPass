<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VaultPass">

        <activity
            android:name="com.vaultpass.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.VaultPass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.vaultpass.activities.OnboardingActivity" />
        <activity android:name="com.vaultpass.activities.CreateMasterPasswordActivity" />
        <activity android:name="com.vaultpass.activities.UnlockActivity" />
        <activity android:name="com.vaultpass.activities.HomeActivity" />
        <activity android:name="com.vaultpass.activities.AddPasswordActivity" />
        <activity android:name="com.vaultpass.activities.PasswordDetailActivity" />
        <activity android:name="com.vaultpass.activities.EditPasswordActivity" />
        <activity android:name="com.vaultpass.activities.SearchActivity" />
        <activity android:name="com.vaultpass.activities.FavoritesActivity" />
        <activity android:name="com.vaultpass.activities.CategoriesActivity" />
        <activity android:name="com.vaultpass.activities.PasswordGeneratorActivity" />
        <activity android:name="com.vaultpass.activities.SecurityActivity" />
        <activity android:name="com.vaultpass.activities.BackupRestoreActivity" />
        <activity android:name="com.vaultpass.activities.SettingsActivity" />
        <activity android:name="com.vaultpass.activities.AboutActivity" />
        <activity android:name="com.vaultpass.activities.ProfileActivity" />
        <activity android:name="com.vaultpass.activities.ChangeMasterPasswordActivity" />
        <activity android:name="com.vaultpass.activities.LockScreenActivity" />

        <!-- Existing activities from template if needed, or remove them -->
        <activity android:name="com.example.demo.MainActivity" />

    </application>

</manifest>