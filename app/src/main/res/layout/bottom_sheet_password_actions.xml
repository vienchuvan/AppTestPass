<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@color/surface">

    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center"
        android:layout_marginBottom="24dp"
        android:background="@color/outline"
        android:alpha="0.3" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_bs_copy_username"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Copy Username"
        android:gravity="start|center_vertical"
        app:icon="@drawable/ic_vault_logo" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_bs_copy_password"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Copy Password"
        android:gravity="start|center_vertical"
        app:icon="@drawable/ic_vault_logo" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_bs_edit"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Edit"
        android:gravity="start|center_vertical"
        app:icon="@drawable/ic_vault_logo" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_bs_delete"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:textColor="@color/error"
        android:gravity="start|center_vertical"
        app:icon="@drawable/ic_vault_logo"
        app:iconTint="@color/error" />

</LinearLayout>